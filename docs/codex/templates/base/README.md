# Base Scaffold

This surface defines the universal base scaffold for governed repositories.

The base scaffold is stack-agnostic. It establishes the minimum reusable directory contract that every governed repository should preserve before any specialization overlay is applied.

Canonical expectations include:

- explicit docs-root governance surfaces
- explicit governance tooling surfaces
- explicit template discovery surfaces
- explicit evidence surfaces
