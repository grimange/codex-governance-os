# CLI And Worker Overlay

This overlay adds command-line and worker-oriented runtime expectations on top of the universal base scaffold without changing governance core semantics.

It is intended for repositories whose primary runtime is command, batch, task, queue, or worker execution rather than HTTP routing.
