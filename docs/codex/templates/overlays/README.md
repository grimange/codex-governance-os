# Overlay Scaffold Surface

This surface contains stack-aware and topology-aware overlays that extend the universal base scaffold.

Overlays may add runtime-specific directories and helper conventions, but they must not redefine governance core semantics or move canonical governance surfaces without a separate governance pipeline.
