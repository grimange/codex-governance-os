# Governance Capability Execution Map

This document maps governance capabilities to the pipeline lanes and canonical
surfaces that implement or are expected to implement them.

## Governance Doctrine

Implemented by:

- foundational governance establishment and verification lanes in the active
  governance corpus
- canonical doctrine surfaces under `docs/governance/`

## Pipeline Governance

Implemented by:

- registered governance pipeline corpus in `docs/pipelines/registry/pipeline-registry.md`
- centralized execution-history and analytics lanes including pipelines `139`
  through `148`

## Execution Governance

Implemented by:

- governed execution and verification lanes across the governance corpus
- session admission, activation, runtime-boundary, orchestration, and handoff
  doctrine surfaces

## Observability

Implemented by:

- pipeline-run ledger and analytics lanes
- session evidence interpretation, reconstruction, packaging, and continuity
  lanes

## Governance Intelligence

Implemented by:

- pipeline `149` establish governance maturity scoring surface
- pipeline `150` verify governance maturity scoring surface
- pipeline `151` establish governance maturity trend tracking
- pipeline `152` verify governance maturity trend tracking
- pipeline `153` establish governance gap analysis engine
- pipeline `154` verify governance gap analysis engine
- pipeline `155` normalize governance gap analysis engine structure
- pipeline `156` establish governance capability advancement planner
- pipeline `157` verify governance capability advancement planner
- pipeline `158` generate governance capability advancement plan
- pipeline `159` verify governance capability advancement plan

## Governance Capability Advancement Tracking

Implemented by:

- pipeline `160`

## Governance State Introspection

Planned implementation pipelines:

- pipeline `161`
- pipeline `162`
- pipeline `163`
- pipeline `164`

## Autonomous Governance Loop

Planned implementation pipelines:

- not yet registered in canonical repository state

## Architecture Advisor

Planned implementation pipelines:

- not yet registered in canonical repository state
