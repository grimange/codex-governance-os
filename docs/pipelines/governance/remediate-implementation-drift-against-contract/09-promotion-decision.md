# Promotion Decision

`PROMOTE_WITH_OBSERVATIONS`

## Basis

- The major contract violation identified by pipeline `005` has been corrected in canonical repository state.
- Current active governance coverage is now published through the registry for both `005` and `006`.
- Remaining drift is limited to descriptive `PROPOSED` labels in active pipeline definitions and does not prevent registry-compliance verification.
