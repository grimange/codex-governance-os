# Verified Continuity Scenario

`scenario_id`: `continuity.verified.explicit-predecessor-and-artifact-chain`

Participating sessions:

- `session_alpha`
- `session_beta`

Admissible evidence:

- explicit predecessor-successor linkage from `session_alpha` to `session_beta`
- one governed artifact continuation reference from `session_beta` back to an
  output of `session_alpha`

Expected classification:

- `VERIFIED_CONTINUITY`

Boundary validation:

- continuity remains explicit and evidence-backed
- each session retains its own canonical `session_id`
- no session narratives are merged

Prohibited reasoning patterns:

- chronology alone
- topic similarity alone
