# Reference Update Surface

## Updated Surfaces

- repository constitution and local operating instructions
- repository entrypoint documentation
- architecture doctrine
- universal template system README
- template registry loader defaults
- template linter default template roots
- admitted template entry metadata and compiled index
- registry tests and verification commands
- prior pipeline `019` implementation artifacts that described the admitted-template layout
- pipeline registry activation catalog

## Residual Tolerance

Historical pipeline definitions may still mention legacy root-level example paths when describing the prior state or the migration problem. Those references are descriptive only and do not define the active routing model.
