# Problem Statement

The foundational governance lane set was authored before a stable universal pipeline template existed.

As a result, the core lanes mixed:

- missing or ad hoc frontmatter
- inconsistent purpose and input sections
- phase-heavy formatting without a common top-level contract
- variable admission and verification semantics

That inconsistency made the foundation set harder to lint, route, and compare mechanically even though the lane intent was already established.
