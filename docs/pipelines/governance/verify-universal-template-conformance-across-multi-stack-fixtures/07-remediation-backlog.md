# Remediation Backlog

| priority | item | classification | rationale |
|----------|------|----------------|-----------|
| medium | add Node or TypeScript service overlay manifest and scaffold surface | missing overlay | broadens supported service ecosystems beyond the current generic `service` overlay |
| medium | add CLI / worker overlay manifest and scaffold surface | missing overlay | makes non-web operational shapes an explicit supported class |
| low | add a dedicated automated conformance CLI that emits a formal per-fixture report | verification harness enhancement | current verification relies on unittest plus scaffold commands rather than a single purpose-built conformance command |
