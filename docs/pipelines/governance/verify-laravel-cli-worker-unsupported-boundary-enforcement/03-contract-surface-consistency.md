# Contract Surface Consistency

The canonical contract authority is [docs/contracts/universal-template-composition-contract.md](/home/ramjf/python-projects/codex-governance-os/docs/contracts/universal-template-composition-contract.md).

That document currently aligns with runtime behavior by:

- listing `laravel + cli-worker` in the certified fail-closed boundary
- stating the specific rationale: `missing Laravel worker composition contract`
- declaring that `laravel` has no admitted multi-overlay compositions at this time, including `cli-worker`

The doctor surface returns the same reason and decision source, so contract and runtime diagnostics are aligned.
