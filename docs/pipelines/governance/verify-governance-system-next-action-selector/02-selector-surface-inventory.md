# Selector Surface Inventory

Verified component:

- `tools/governance/inspect_governance_state.py`

Verified mode:

- `next-action`

Generated surface under verification:

- `docs/governance/governance-system-next-action.json`

Observed canonical output on clean repository state:

- `target_domain`: `multi_agent_governance`
- `recommended_action_type`: `state_normalization`
- `suggested_pipeline`:
  `establish-role-scoped-codex-sub-agent-specialization`

Repository-truth observation:

- the selector output records `derived_from` as
  `docs/governance/governance-system-advancement-roadmap.json`
- the pipeline definition text refers to `governance-system-roadmap.json`
- verification follows repository truth and the established canonical file path
