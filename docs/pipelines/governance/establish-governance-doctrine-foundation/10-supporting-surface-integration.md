# Supporting Surface Integration

## Updated

- `AGENTS.md`
  Reason: elevated the governance doctrine set under `docs/governance/` into the canonical authority order and routing guidance so the doctrine foundation is discoverable from the constitution.
- `.codex/AGENTS.md`
  Reason: instructed agents to use the doctrine foundation operationally for lifecycle, artifact, naming, contract-writing, and terminology rules.
- `docs/governance/architecture-doctrine.md`
  Reason: integrated the doctrine foundation into the repository's doctrinal layer and source-of-truth interpretation model.
- `docs/pipelines/registry/pipeline-registry.md`
  Reason: registered pipeline `008` as an active governance surface.

## Left Unchanged

- Existing pipeline definitions `000` through `007`
  Reason: this pipeline establishes the doctrine foundation and records normalization needs, but it does not rewrite the whole catalog.
- `docs/contracts/pipeline-registry-integrity-contract.md`
  Reason: the contract already fits the new contract-writing standard closely enough to remain valid without immediate revision.
- README surfaces
  Reason: no template README reference was present that needed doctrine wiring.

## Doctrine References Added

- Constitution-level references to the doctrine set in `docs/governance/`
- Local-agent references to lifecycle, artifact, naming, contract-writing, and terminology doctrine surfaces
- Architecture-doctrine references to the doctrine foundation as part of the doctrinal layer

## Residual Embedded Guidance

- Early governance pipelines still contain embedded lifecycle, artifact, and contract-shape guidance that the new doctrine foundation now supersedes as the reusable baseline.
- Several active pipeline definitions still advertise `Status: PROPOSED`, which remains a discoverability inconsistency until later normalization work updates the pipeline bodies themselves.
