# Cross-Session Evidence Types

Pipeline `132` admits only explicit evidence types for continuity validation:

- explicit session predecessor reference
- governance artifact continuation
- artifact chain evidence
- canonical continuation markers

These evidence types support cross-session continuity only when they remain
linked to governed surfaces and explicit `session_id` boundaries.
