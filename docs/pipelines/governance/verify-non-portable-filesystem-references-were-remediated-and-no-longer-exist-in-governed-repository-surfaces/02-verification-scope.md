# Verification Scope

Inspected active governed surfaces:

- canonical governance doctrine under `docs/governance/`
- canonical contracts under `docs/contracts/`
- repository entry surfaces `README.md` and `.codex/AGENTS.md`
- pipeline registry at `docs/pipelines/registry/pipeline-registry.md`
- active pipeline definitions under `docs/pipelines/governance/*--*.md`

Inspected historical or evidence-preserving surfaces:

- pipeline `107` verification bundle
- pipeline `108` remediation bundle
- pipeline `109` invariant-establishment bundle

Scope interpretation:

- active governed surfaces must not contain machine-local live links
- literal forbidden-pattern examples inside active lane definitions may remain
  when they describe disallowed inputs rather than functioning as live links
- historical bundles may preserve older path forms when recording earlier
  defects or scan criteria
