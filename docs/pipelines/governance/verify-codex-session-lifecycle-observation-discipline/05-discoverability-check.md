# Discoverability Check

Verified discoverability:

- `docs/governance/architecture-doctrine.md`
  - lists the lifecycle-observation canon in the reusable governance-law
    surfaces
  - defines the canon as the normalization surface for future lifecycle
    observations
  - records the governance implication that lifecycle observation support must
    follow it
- `.codex/AGENTS.md`
  - routes lifecycle observation ordering, runtime lifecycle reconstruction,
    and normalization questions to the canon
- `README.md`
  - lists the canon in the Layer 6 "Learn More" references
- `docs/pipelines/registry/pipeline-registry.md`
  - records Pipeline `115` as the establishment lane
  - records Pipeline `116` as the verification lane

Restriction:

- The canonical architecture-doctrine discoverability path is
  `docs/governance/architecture-doctrine.md`, and Pipeline `117` normalizes
  the `116` lane text to that repository path.

## Classification

- canonical discoverability in live repository surfaces: `VERIFIED`
- lane-definition path accuracy: `NORMALIZED`
