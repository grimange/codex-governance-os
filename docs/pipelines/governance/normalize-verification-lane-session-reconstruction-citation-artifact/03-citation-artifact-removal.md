# Citation Artifact Removal

Pipeline `123` removes the stray `:contentReference[...]` artifact from the
live `122` verification lane text.

Affected `122` verification-bundle notes were also normalized so they now
describe the artifact as a historical defect closed by Pipeline `123` rather
than as a current live restriction.
