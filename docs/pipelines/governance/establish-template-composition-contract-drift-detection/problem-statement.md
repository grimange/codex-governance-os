# Problem Statement

The universal template composition subsystem already had certified contract, runtime enforcement, explainability, and cross-surface consistency verification.

The remaining gap was self-protection against future drift between:

- the Markdown contract
- the runtime decision engine
- manifest compatibility declarations
- scaffold enforcement
- listing surfaces

Pipeline `032` closes that gap by installing an explicit drift-detection layer and simulation coverage.
