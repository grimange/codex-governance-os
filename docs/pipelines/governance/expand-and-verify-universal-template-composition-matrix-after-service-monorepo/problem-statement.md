# Problem Statement

Pipeline `036` promoted `service + monorepo` into the certified supported matrix.

That was the second structural matrix expansion, so the entire composition system required another full verification pass to confirm that:

- all supported compositions still admit correctly
- remaining rejected compositions still fail closed
- scaffold placement rules remain deterministic
- contract, doctor, manifest, and listing surfaces remain aligned

Pipeline `037` records that post-expansion re-verification.
