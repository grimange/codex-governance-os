# Evidence-Scoped Decision Boundary

The selector remains bounded by upstream canonical surfaces.

Current bounded behavior:

- it selects `multi_agent_governance` because the roadmap already marks it as
  `recommended_next_target`
- it preserves the concrete suggested pipeline already present in the
  remediation plan
- it does not invent a new capability, pipeline, or action type beyond the
  upstream evidence

If upstream remediation linkage were `unknown`, the selector would preserve that
value rather than inventing a stronger recommendation.
