# Remediation Linkage Boundary

Pipeline 168 keeps remediation linkage bounded by repository truth.

Observed linkage:

- `multi_agent_governance`
  - suggested pipeline:
    `establish-role-scoped-codex-sub-agent-specialization`
- `architecture_advisor`
  - suggested pipeline:
    `unknown`
- `autonomous_governance_loop`
  - suggested pipeline:
    `unknown`

The planner records `unknown` rather than inventing unsupported concrete
pipelines when canonical evidence does not already support a specific lane.
