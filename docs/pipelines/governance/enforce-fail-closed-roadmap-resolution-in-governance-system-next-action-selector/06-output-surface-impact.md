# Output Surface Impact

Valid-state output remains unchanged in canonical repository truth:

- `target_domain`: `multi_agent_governance`
- `recommended_action_type`: `state_normalization`
- `suggested_pipeline`:
  `establish-role-scoped-codex-sub-agent-specialization`

Invalid-state impact:

- canonical next-action output is not written
- machine-readable failure is emitted instead

This preserves trustworthy canonical output semantics.
