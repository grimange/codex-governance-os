# Normalization

Applied corrections:

- replaced the non-canonical doctrine path in the `116` lane definition with
  `docs/governance/architecture-doctrine.md`
- updated the affected `116` verification artifacts so they no longer contain
  the incorrect repository path
- preserved the historical `116` verdict token while converting the path-level
  note into a normalization reference instead of a live wrong-path citation

No Layer 6 authority ordering or interpretation changed.
