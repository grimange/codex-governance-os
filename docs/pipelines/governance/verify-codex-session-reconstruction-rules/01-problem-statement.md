# Problem Statement

Pipeline `121` established
`docs/governance/codex-session-reconstruction-rules.md` as the canonical Layer
6 doctrine for deterministic session reconstruction.

Pipeline `122` verifies that the reconstruction rules:

- remain subordinate to the session evidence-interpretation model
- preserve state-machine, ledger, registry, observation, and runtime-boundary
  authority boundaries
- remain documentation-level and runtime-neutral
- are discoverable from the required repository entry surfaces

This is a documentation-level governance verification lane only.
