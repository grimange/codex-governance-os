# Current Supported Composition Matrix

## Overlay Set

The current overlay inventory is:

- `cli-worker`
- `django`
- `laravel`
- `monorepo`
- `node-typescript-service`
- `php-package`
- `python-package`
- `service`

This produces `28` unique overlay pairs.

## Supported Multi-Overlay Pairs

The doctor surface and canonical contract currently admit `7` supported pairs:

- `cli-worker + monorepo`
- `cli-worker + node-typescript-service`
- `cli-worker + php-package`
- `cli-worker + python-package`
- `django + monorepo`
- `monorepo + node-typescript-service`
- `monorepo + service`

These align exactly with the certified supported multi-overlay matrix in [docs/contracts/universal-template-composition-contract.md](/home/ramjf/python-projects/codex-governance-os/docs/contracts/universal-template-composition-contract.md).
