# Problem Statement

The governance OS had certified compound composition only up to three overlays.
The candidate four-overlay stack
`cli-worker + monorepo + python-package + scheduler` already had all of its
lower-order edges certified, but it was not yet admitted as a governed contract.

This lane establishes that first quadruple-overlay contract explicitly rather
than inferring it from the supported triples.
