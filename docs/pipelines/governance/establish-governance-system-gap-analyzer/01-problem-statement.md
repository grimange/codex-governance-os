# Problem Statement

The governance system can now expose state and system-level maturity, but it
still cannot emit a canonical machine-readable inventory of specific governance
gaps.

Without that analyzer, invalid domains and unverified capability areas remain
visible only as maturity output, not as explicit gap records with evidence,
severity, and remediation linkage.
