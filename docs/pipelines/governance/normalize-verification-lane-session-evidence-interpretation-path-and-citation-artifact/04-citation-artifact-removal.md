# Citation Artifact Removal

Pipeline `120` removes the stray `:contentReference[...]` artifact from the
live `119` lane text.

The normalization preserves the meaning of the runtime-neutrality section while
removing a non-repository citation fragment that did not belong in governed
documentation.
