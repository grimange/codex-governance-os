# Canonical Path Normalization

Pipeline `120` normalizes the `119` verification lane so the state-machine
reference now points to:

- `docs/contracts/codex-session-state-machine-canon.md`

Affected verification-bundle notes were also updated so they no longer treat
the wrong root path as a live restriction in current repository state.
