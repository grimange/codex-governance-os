# Problem Statement

Pipeline `087` established the first canonical role-scoped Codex
specialization surface, but it did so without verification. Before the role
model can be treated as a reliable governance layer, the repository must verify
that the model is complete, subordinate to Layers 0 through 3, discoverable,
and explicit about its non-claims.
