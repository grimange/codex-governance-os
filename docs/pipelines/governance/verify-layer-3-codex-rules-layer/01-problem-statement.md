# Problem Statement

Pipeline `085` established the first canonical Layer 3 Codex rules surface, but
did so explicitly without verification. Before the repository can rely on Layer
3 as an operational control surface for Codex behavior, it must verify that the
canon is discoverable, authority-consistent, restriction-preserving, and
non-mutating relative to Layers 0 through 2.
