# Problem Statement

The repository has verified snapshot integrity and a snapshot-bound authoritative
next-action selector, but it still lacks a single canonical endpoint that
answers overall governance state in one place.

Without that answer surface, governance status remains fragmented across
multiple intermediate JSON artifacts and a selector output that only answers the
next step, not the full current governance condition.
