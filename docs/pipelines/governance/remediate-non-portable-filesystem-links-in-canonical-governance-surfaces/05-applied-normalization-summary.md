# Applied Normalization Summary

Applied changes:

- converted absolute filesystem markdown links in the active Layer 6 doctrine,
  session registry, session ledger, session handoff packet contract, and
  handoff README to portable relative links
- converted absolute filesystem links in the pipeline `107` verification scope,
  layer-authority note, discoverability note, and final verdict to portable
  relative links
- corrected the active pipeline `107` definition so the `affects` list now
  names `docs/contracts/codex-session-state-machine-canon.md`
- registered pipeline `108` in the pipeline registry with canonical definition
  and artifact bundle paths

Meaning preserved:

- the `107` verification bundle still records that, at verification time, the
  lane text named the state-machine canon under `docs/governance/`
- the runtime-implementation restriction in `107` remains unchanged
- no Layer 6 doctrine semantics were broadened or rewritten beyond path and
  link normalization
