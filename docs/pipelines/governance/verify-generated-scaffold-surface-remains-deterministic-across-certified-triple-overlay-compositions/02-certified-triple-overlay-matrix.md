# Certified Triple-Overlay Matrix

The live certified triple-overlay matrix verified by this lane is:

- `cli-worker + monorepo + python-package`
- `cli-worker + monorepo + scheduler`
- `cli-worker + python-package + scheduler`
- `laravel + monorepo + scheduler`

These combinations are drawn from the current certified runtime matrix and
compound composition ledger, not from historical example text.
