# Canonical Template Composition Contract

The canonical contract published by pipeline `028` is:

- `docs/contracts/universal-template-composition-contract.md`

That contract is now the authoritative documentation surface for the certified universal template composition boundary.
