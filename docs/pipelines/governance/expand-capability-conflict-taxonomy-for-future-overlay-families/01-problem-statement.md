# Problem Statement

The capability-based composition system was already operational, but conflict reasoning still relied on a mix of free-text explanations and coarse decision classes.

That was sufficient for the initial overlay set, but it would not scale cleanly as future overlay families introduce additional worker, runtime, package, entrypoint, and orchestration boundaries.
