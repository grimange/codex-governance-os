# Problem Statement

Layers 0 through 2 now define doctrine, interpretation, and execution
boundaries, but the repository still lacked one canonical surface stating how
Codex itself must behave on top of those layers. Without that Layer 3 surface,
Codex behavior remained partially implicit across `AGENTS.md`,
`.codex/AGENTS.md`, and scattered repository practice.
