# Current Certified Composition Evidence

At lane entry, repository evidence already established the following certified
compound compositions:

- `cli-worker + monorepo + python-package`
- `cli-worker + monorepo + scheduler`
- `cli-worker + python-package + scheduler`
- `laravel + monorepo + scheduler`

At lane entry, the following triple-overlay boundary already remained closed:

- `django + monorepo + scheduler`

Additional worker-oriented framework scheduler compounds also remained closed by
the certified matrix and role model:

- `django + cli-worker + scheduler`
- `laravel + cli-worker + scheduler`

The gap was not runtime uncertainty. The gap was missing central governance
normalization for compound certification and fail-closed triple boundaries.
