# Evidence Package Definition

Pipeline `128` establishes the session reconstruction evidence package as the
complete collection of artifacts used to evaluate one session reconstruction
case.

Every package must declare:

- `session_id`
- package scope
- reconstruction objective
- evaluation timestamp
- evaluator context

The package is an evidence-organization surface for one case, not a
replacement case model or evaluation engine.
