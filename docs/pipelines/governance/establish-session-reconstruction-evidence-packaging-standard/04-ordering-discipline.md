# Ordering Discipline

Pipeline `128` requires deterministic ordering for packaged reconstruction
evidence.

The canonical ordering is:

1. case identity
2. source session evidence
3. governance doctrine citations
4. pipeline artifact references
5. reconstruction assumptions
6. restriction record

This ordering must not vary arbitrarily across cases.
